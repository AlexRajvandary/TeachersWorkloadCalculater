<Window x:Class="StudingWorkloadCalculator.Windows.TeacherWorkLoad"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudingWorkloadCalculator.Windows"
        xmlns:uc="clr-namespace:StudingWorkloadCalculator.UserControls"
        mc:Ignorable="d"
        Title="TeacherWorkLoad" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.ColumnSpan="2" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:TeacherWorkLoad}, Path=TeachersWorkload.Teacher.Subjects}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Всего"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RelativeSource={RelativeSource AncestorType=local:TeacherWorkLoad}, Path=TeachersWorkload.Workload.Total}"/>

    </Grid>
</Window>
